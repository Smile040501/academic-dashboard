const enums = `
    enum CourseType {
        COMMON
        PM
        PME
        HSE
        SME
        PMT
        OE
        PROJECT
    }

    enum SemesterType {
        FALL
        SPRING
        SUMMER
    }

    enum CurriculumType {
        MAJOR
    }
`;

export default enums;
