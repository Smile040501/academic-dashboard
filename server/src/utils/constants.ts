export const ADMIN_EMAIL = "";

export const JWT_ACCESS_TOKEN_EXPIRATION_STR = "1h";
export const JWT_ACCESS_TOKEN_EXPIRATION = 3600;

export const MAX_REQUESTS = 100; // limit each IP to 100 requests per windowMs
export const WINDOW_MS = 15 * 60 * 1000; // 15 minutes
